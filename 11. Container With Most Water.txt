## 11. Container With Most Water
-11. Container With Most Water [Array] [Two Pointers] [Medium]

> Given n non-negative integers a1, a2, ..., an, where each represents a point at coordinate (i, ai). n vertical lines are drawn such that the two endpoints of line i is at (i, ai) and (i, 0). Find two lines, which together with x-axis forms a container, such that the container contains the most water.
> Note: You may not slant the container and n is at least 2.

### 题目
给定n个非零整数a1, a2, ..., an，每个数代表坐标为(i, ai)的点，我们画n条垂直线使得第i条线的两端点为(i, ai)和(i, 0)。从中找出两条线，和x轴一起构成一个容器，使得该容器能容纳最多的水。
### 解法
左右夹逼，每次把较短的一边往里移动一步。时间复杂度$O(n)$，空间复杂度$O(1)$
### 代码
``` C++
class Solution {
public:
    int maxArea(vector<int>& height) {
        int start = 0;
        int end = height.size() - 1;
        int result = INT_MIN;
        while (start < end) {
            int area = min(height[end], height[start]) * (end - start);
            result = max(result, area);
            if (height[start] <= height[end]) {
                start++;
            } else {
                end--;
            }
        }
        return result;
    }
};
```

### 相关题目
-11. Container With Most Water [Array] [Two Pointers] [Medium]
-42. Trapping Rain Water [Array] [Stack] [Two Pointers]
-84. Largest Rectangle in Histogram [Array] [Stack]
